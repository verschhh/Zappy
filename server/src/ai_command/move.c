/*
** EPITECH PROJECT, 2022
** Zappy
** File description:
** move.c
*/

#include "../../includes/zappy.h"

// int forward(int sockfd, serv_t *serv, char *buffer)
// {
    
// }

// int inventory(int sockfd, serv_t *serv, char *buffer)
// {
//     client_t *client = get_correct_client(serv, sockfd);
//     char *msg = NULL;
//     int len = snprintf(msg, 0, "[food %d, linemate %d, deraumere %d, sibur %d,
//         mendiane %d, phiras %d, thystame %d]\n", tmp->food, tmp->linemate,
//         tmp->deraumere, tmp->sibur, tmp->mendiane, tmp->phiras, tmp->thystame);
//     msg = malloc(sizeof(char) * len + 1);
//     msg[len] = '\0';
//     snprintf(msg, len, "[food %d, linemate %d, deraumere %d, sibur %d,
//         mendiane %d, phiras %d, thystame %d]\n", tmp->food, tmp->linemate,
//         tmp->deraumere, tmp->sibur, tmp->mendiane, tmp->phiras, tmp->thystame);
//     write(sockfd, msg, len);
//     return 0;
// }